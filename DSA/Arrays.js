// Array time complexity on methods
// Search: takes O(1) time
// Push: takes O(1) time
// Splice and Shift/Unshift: takes O(n) time to modify/add elements to an array
// Pop: takes O(1) time
// Delete: takes O(n) time because we will be shifting elements over when one is deleted
// Slice: takes
